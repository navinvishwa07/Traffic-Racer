#pragma once

const int WINDOW_WIDTH = 800;
const int WINDOW_HEIGHT = 600;

const float LANE_LEFT = 215.f;
const float LANE_MIDDLE = 400.f;
const float LANE_RIGHT = 585.f;

const float PLAYER_Y = 500.f;
const float LANE_SMOOTHING = 10.0f; 
